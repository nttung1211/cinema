import React, { FC } from 'react';

interface Props {}

const Browse: FC<Props> = () => {
  return (
    <div>
      <h1>Browse component is working</h1>
    </div>
  );
};

export default Browse;
